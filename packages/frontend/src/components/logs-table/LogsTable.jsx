import Typography from '@mui/material/Typography';

const LogsTable = ({ realtimeData, collectedData }) => {

	console.log(realtimeData, collectedData)
	return (
		<div>
		<Typography >LogsTable </Typography>
		</div>
	);
};

export default LogsTable;
